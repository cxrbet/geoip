name: Build Custom GeoIP Files

on:
  workflow_dispatch:  # 保留手动触发
  push:              # 保留推送触发
    branches:
      - master
    paths-ignore:
      - ".gitignore"
      - "LICENSE*"
      - "*.md"

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout codebase
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ./go.mod

      - name: Set variables
        run: |
          echo "TAG_NAME=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV
          echo "RELEASE_NAME=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV
        shell: bash

      - name: Build Custom GeoIP
        run: |
          echo "Current directory contents:"
          ls -la
          go build ./
          ./geoip convert -c ./config.json || { echo "Convert failed"; exit 1; }
          echo "Files after conversion:"
          ls -la

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: custom-geoip-files
          path: "customip.dat"

      - name: Generate sha256 checksum for dat files
        run: |
          cd ./ || exit 1
          sha256sum customip.dat > ./customip.dat.sha256sum

      - name: Upload files to GitHub release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file_glob: true
          file: ./customip.dat ./customip.dat.sha256sum
          release_name: ${{ env.RELEASE_NAME }}
          tag: ${{ env.TAG_NAME }}
